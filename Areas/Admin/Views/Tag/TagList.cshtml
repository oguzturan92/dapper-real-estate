@model List<ResultTagDto>
@{
    ViewData["Title"] = "TagList";
    Layout = "_LayoutAdmin";
    int count = 0;
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Etiket Listesi</h4>
            <div class="table-responsive pt-3">
                @if (Model.Any())
                {
                    <table class="table table-bordered mb-3">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Ad</th>
                                <th colspan="2"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td>@item.TagName</td>
                                    <td><a class="btn btn-inverse-warning" asp-area="Admin" asp-controller="Tag" asp-action="TagUpdate" asp-route-id="@item.TagId">Güncelle</a></td>
                                    <td><a class="btn btn-inverse-danger" asp-area="Admin" asp-controller="Tag" asp-action="TagDelete" asp-route-id="@item.TagId">Sil</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                } else
                {
                    <div class="alert alert-primary" role="alert">
                        Öge mevcut değil
                    </div>
                }
                <a class="btn btn-inverse-success" asp-area="Admin" asp-controller="Tag" asp-action="TagCreate">Ekle</a>
            </div>
        </div>
    </div>
</div>